#include <stdio.h>
#include <assert.h>

//#define CLEAR2CONTINUE
#include "../include/traces.h" 

// C'est dans le fichier elt.h qu'on doit choisir l'implémentation des T_elt
#include "elt.h"
#include "avl.h"

int main(ELT_STRING nom_fichier, int n) {
	T_avl root = NULL; 
	T_avlNode * pAux = NULL;

	T_elt srch; 

	CLRSCR();
	WHOAMI(); 
  
  int i;
  
  for(i=0,i<n,i++){
    FILE* fichier=NULL;
    fichier=fopen("nom_fichier","r");
    while(fgets
    
    
  }

	return 0;
}

